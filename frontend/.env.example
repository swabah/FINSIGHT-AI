# Frontend Environment Variables
# Copy this file to .env (dev) or set in Vercel dashboard (production)

# Backend API URL — set to your deployed backend (e.g. Railway / Render) in production
VITE_API_URL=http://localhost:5000/api

# Frontend URL (used for CORS, OAuth redirects, etc.)
VITE_FRONTEND_URL=http://localhost:5174
